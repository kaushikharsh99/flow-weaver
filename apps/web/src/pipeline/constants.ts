// ─── Layout ─────────────────────────────────────────────────────
export const SIDEBAR_DEFAULT_WIDTH = 280;
export const SIDEBAR_MIN_WIDTH = 56;
export const SIDEBAR_MAX_WIDTH = 500;

export const GRID_SIZE = 15;

export const COMMENT_NODE_DEFAULT_WIDTH = 220;
export const COMMENT_NODE_DEFAULT_HEIGHT = 120;

// ─── History ────────────────────────────────────────────────────
export const HISTORY_MAX_SIZE = 50;

// ─── Pipeline Format ────────────────────────────────────────────
export const PIPELINE_SCHEMA_URL = 'https://flowweaver.dev/schemas/pipeline/v1.json';
export const PIPELINE_FORMAT_VERSION = '1.0.0';

// ─── Execution ──────────────────────────────────────────────────
export const MOCK_EXECUTION_MIN_DELAY_MS = 300;
export const MOCK_EXECUTION_MAX_DELAY_MS = 900;
export const MOCK_ERROR_RATE = 0.05;

// ─── Node Defaults ──────────────────────────────────────────────
export const NODE_ADD_OFFSET_RANGE = { x: 100, y: 100 };
export const NODE_ADD_BASE_POSITION = { x: 280, y: 200 };
export const NODE_PASTE_OFFSET = { x: 40, y: 40 };

// ─── Recent Node Types ──────────────────────────────────────────
export const MAX_RECENT_NODE_TYPES = 8;
